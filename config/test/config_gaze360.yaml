---
test:

    load:
        begin_step: 10
        end_step: 400
        steps: 10

    data: 
        image: "/data1/lyx/dataset/gaze360/Image"
        label: "/data1/lyx/dataset/gaze360/Label/test.label"
        name: gaze360
        header: True
        isFolder: False

    savename: "evaluation"

    # device: 0

    reader: reader

# dropout = 0
# dim_feed = 512
